// Variables are simply containers that stores values
// Tehy follow this patter:
var yourVariableName = yourValue;

// They can store all of the values we've seen
var name = "Rusty";
var secretNumber = 73;
var isAdorable = true;

// Recall the stored value by calling the varibale name
var name = "Rusty";
"hello there " + name;

// Now var -> let & const
// Prefer const to let
// Prefer let to var
// Almost never use var

// Useful Build-in Method

// Pop out a alert message to the user
alert("Alert Message");
// Print out something in the console
console.log(9);
// Require an input from user
prompt("Prompt message");
